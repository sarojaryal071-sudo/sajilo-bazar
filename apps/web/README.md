# Sajilo Bazar — web

React + Vite frontend. See the root `README.md` and `docs/` for the full spec.

```bash
npm run dev      # starts on http://localhost:5173, proxies /api to :4000
npm run build
```
